# Minimum session is independent of cadence

Minimum session prevents fragments caused by any unavailable boundary, not only cadence breaks. It stays active when focus cadence is disabled; disabling cadence removes target-focus breaks but does not permit arbitrary short work fragments before meetings or fixed breaks. A separate toggle would expand a simple preference with little benefit and make schedule behavior harder to explain.
