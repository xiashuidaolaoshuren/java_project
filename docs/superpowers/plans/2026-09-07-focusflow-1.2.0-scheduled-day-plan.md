# Implementation plan: FocusFlow 1.2.0 the scheduled day

**Spec:** `docs/superpowers/specs/2026-09-07-focusflow-1.2.0-scheduled-day-design.md`  
**Parent spec:** `docs/superpowers/specs/2026-05-14-focusflow-ai-design.md`  
**ADRs:** `docs/adr/0002` through `docs/adr/0025`  
**Glossary:** `CONTEXT.md`  
**Created:** 2026-09-22  
**Subsystem scope:** The scheduled day (one product milestone: preferences, commitments, pure scheduler, plan cutover, and the day UI)

## Summary

Ship milestone 1.2.0 so a Daily plan is a minute-aligned clock schedule. Users keep in-code scheduling defaults until they save preferences. They can store dated commitments. Generation asks the model for a total ordering of at most 100 plannable tasks, then a two-stage Java scheduler places that order into the work window, reserves the latest free minutes as buffer, and snapshots each ranked task once. Scheduled blocks reference that snapshot. Regenerating a date compare-and-replaces the one plan for that owner and date and issues a new plan id. The dashboard and plan detail render a vertical time rail.

Out of scope: recorded actuals, carry-over, mid-day re-plan, peak-aware placement, timezones, overnight windows, calendar sync, recurring commitments or tasks, live timer, drag-to-reschedule, manual block editing, task dependencies, and the multi-day Gantt. Do not change authentication, CSRF, Flyway's baseline policy, or 1.1.0 observability except the bounded ranking-rejection reasons the spec adds.

This is one milestone, not separate product specs. Preferences and commitments exist to feed generation; the scheduler has no user-facing surface until generation persists it; the UI consumes that contract. Slices below match the spec's five steps, split only where a later slice would otherwise break the suite or share too many files to plan in one session.

## Discovery notes

- Reuse: owner-scoped services, `TaskQueryService` instead of plan-to-task repository access, `DailyPlanPersister` for the short transactional save, `ConflictException` → 409, `BadRequestException` → 400, `NotFoundException` → 404, `ApiErrorResponse` with `@JsonInclude(NON_NULL)`, cookie session plus CSRF, `PostgresIntegrationTest` Testcontainers, ArchUnit `LayeredArchitectureTest`, `apiRequest` 204 → `undefined`, React Query invalidation in `features/plans/hooks.ts`, confirm/toast/alert patterns from the UI spec.
- Constraints: Java 21, Spring Boot 3.4, PostgreSQL `time(0)` / `date`, Hibernate `ddl-auto: validate` after Flyway. Repositories must not depend on `..dto..`. `com.focusflow.schedule` must not depend on Spring, JPA, or `plan.dto`. First-party API: frontend and backend ship together, but backend slices can land first on this branch. `V1__baseline.sql` stays untouched.
- Patterns to follow: one package per feature (`plan`, `task`, `ai`), controller delegates to service, service owns the transaction boundary, cross-feature reads go through a facade. Frontend features live under `frontend/src/features/<feature>/` with `api.ts`, `hooks.ts`, and Vitest beside the component. New `/api/**` routes are already authenticated by `anyRequest().authenticated()`.
- Anti-goals: do not rewrite `V1`. Do not add a PostgreSQL exclusion constraint. Do not lock task CRUD. Do not infer "today" on the server. Do not keep `availableMinutes`, `/latest`, or a persisted warning bit. Do not copy task descriptions into plan snapshots. Do not invent a third placement rule for a short sliver. Do not parallelize subtasks that edit the same service, migration, or `DailyPlanView`.

**Migration sequencing:** the spec's end-state schema is one reshape. This plan lands it as two Flyway versions so each subtask can stay green. `V2` adds preferences, commitments, and the estimate constraint without touching plan rows. `V3` deletes pre-1.2.0 plans and replaces `daily_plan_items` in the same change as the generate rewrite. Concatenating them into one version would make preferences untestable until the plan aggregate is gone.

## File map



### Slice A — Additive schema, owner lock, and task estimates


| Path                                                                     | Create/Modify | Responsibility                                                                                                 | Public surface       |
| ------------------------------------------------------------------------ | ------------- | -------------------------------------------------------------------------------------------------------------- | -------------------- |
| `backend/src/main/resources/db/migration/V2__scheduling_foundations.sql` | create        | Preferences, fixed breaks, commitments, and `tasks.estimated_minutes` null-or-positive. No plan-table changes. | Flyway V2            |
| `backend/src/main/java/com/focusflow/user/UserRepository.java`           | modify        | Pessimistic owner-row lock.                                                                                    | `findByIdForUpdate`  |
| `backend/src/main/java/com/focusflow/user/OwnerSchedulingLock.java`      | create        | One entry point that locks the current owner's row inside the caller's transaction.                            | `lockCurrentOwner()` |
| `backend/src/main/java/com/focusflow/task/Task.java`                     | modify        | Estimate stays nullable; database check is the backstop.                                                       | entity               |
| `backend/src/main/java/com/focusflow/task/dto/CreateTaskRequest.java`    | modify        | Reject zero and negative estimates.                                                                            | request DTO          |
| `backend/src/main/java/com/focusflow/task/dto/UpdateTaskRequest.java`    | modify        | Same estimate rule.                                                                                            | request DTO          |
| `backend/src/main/java/com/focusflow/task/TaskService.java`              | modify        | Persist null or positive estimates only.                                                                       | create/update        |
| `README.md`                                                              | modify        | Say V2 normalizes non-positive estimates to null. The destructive plan warning lands with V3.                  | contributor docs     |




### Slice B — Scheduling preferences


| Path                                                                                     | Create/Modify | Responsibility                                                                       | Public surface                          |
| ---------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------ | --------------------------------------- |
| `backend/src/main/java/com/focusflow/preferences/SchedulingPreferences.java`             | create        | One aggregate per owner. Missing row means defaults; generating does not insert one. | entity                                  |
| `backend/src/main/java/com/focusflow/preferences/FixedBreak.java`                        | create        | Labelled break wholly inside the saved work window.                                  | entity                                  |
| `backend/src/main/java/com/focusflow/preferences/SchedulingPreferencesRepository.java`   | create        | Owner lookup. No DTO types.                                                          | repository                              |
| `backend/src/main/java/com/focusflow/preferences/SchedulingPreferenceDefaults.java`      | create        | In-code defaults from the spec.                                                      | effective preferences                   |
| `backend/src/main/java/com/focusflow/preferences/SchedulingPreferencesService.java`      | create        | Effective GET, validating PUT, owner lock, full fixed-break replacement.             | service                                 |
| `backend/src/main/java/com/focusflow/preferences/SchedulingPreferencesController.java`   | create        | HTTP boundary.                                                                       | `GET`/`PUT /api/scheduling-preferences` |
| `backend/src/main/java/com/focusflow/preferences/dto/SchedulingPreferencesRequest.java`  | create        | Minute-aligned write model.                                                          | request DTO                             |
| `backend/src/main/java/com/focusflow/preferences/dto/SchedulingPreferencesResponse.java` | create        | Effective values plus `persisted`.                                                   | response DTO                            |
| `backend/src/test/java/com/focusflow/architecture/LayeredArchitectureTest.java`          | modify        | New repositories stay off `..dto..`.                                                 | ArchUnit                                |




### Slice C — Commitments


| Path                                                                         | Create/Modify | Responsibility                                                               | Public surface     |
| ---------------------------------------------------------------------------- | ------------- | ---------------------------------------------------------------------------- | ------------------ |
| `backend/src/main/java/com/focusflow/commitment/Commitment.java`             | create        | Dated unavailable window. Not a Task.                                        | entity             |
| `backend/src/main/java/com/focusflow/commitment/CommitmentRepository.java`   | create        | Owner-and-date queries.                                                      | repository         |
| `backend/src/main/java/com/focusflow/commitment/CommitmentService.java`      | create        | Range list, create, move, delete. Lock owner, then reject same-date overlap. | service            |
| `backend/src/main/java/com/focusflow/commitment/CommitmentController.java`   | create        | HTTP boundary.                                                               | `/api/commitments` |
| `backend/src/main/java/com/focusflow/commitment/dto/CommitmentRequest.java`  | create        | Title, date, and window.                                                     | request DTO        |
| `backend/src/main/java/com/focusflow/commitment/dto/CommitmentResponse.java` | create        | Owner-scoped commitment.                                                     | response DTO       |




### Slice D — Pure scheduler


| Path                                                                               | Create/Modify | Responsibility                                                                            | Public surface |
| ---------------------------------------------------------------------------------- | ------------- | ----------------------------------------------------------------------------------------- | -------------- |
| `backend/src/main/java/com/focusflow/schedule/FreeIntervalPlanner.java`            | create        | Stage 1: subtract breaks and commitments, then reserve the latest free minutes as buffer. | pure function  |
| `backend/src/main/java/com/focusflow/schedule/WorkPlacementPlanner.java`           | create        | Stage 2: place ranked tasks with cadence and the minimum-session rule.                    | pure function  |
| `backend/src/main/java/com/focusflow/schedule/SchedulePostconditionValidator.java` | create        | Reject overlap, window escape, and estimate-conservation failures before persistence.     | pure function  |
| `backend/src/main/java/com/focusflow/schedule/*` records                           | create        | Immutable window, interval, ranked-task, block, and result types. No JPA annotations.     | package API    |
| `backend/src/test/java/com/focusflow/architecture/LayeredArchitectureTest.java`    | modify        | `schedule` does not depend on Spring, JPA, or `plan.dto`.                                 | ArchUnit       |


Type-2 may split the records across files. It must not put scheduling policy back into `DailyPlanService`.

### Slice E — Ranking contract, kept off the live generate path until cutover


| Path                                                                      | Create/Modify | Responsibility                                                                                                                                  | Public surface  |
| ------------------------------------------------------------------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| `backend/src/main/java/com/focusflow/plan/DailyPlanRankingValidator.java` | modify        | Add ordered-id validation: unknown, missing, duplicate, block order. Leave the current `availableMinutes` method in place until T10 deletes it. | `validateOrder` |
| `backend/src/main/java/com/focusflow/plan/RankingRejectionReason.java`    | modify        | Add `DUPLICATE_TASK` and `MISSING_OPTIONAL`.                                                                                                    | metric tags     |
| `backend/src/main/java/com/focusflow/ai/DailyPlanPromptBuilder.java`      | modify        | Ask for every id once, in block order. Truncate descriptions to 500 characters in the prompt only. Do not switch `DailyPlanService` here.       | prompt text     |




### Slice F — Plan cutover and generation


| Path                                                                           | Create/Modify | Responsibility                                                                                                                                        | Public surface   |
| ------------------------------------------------------------------------------ | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `backend/src/main/resources/db/migration/V3__scheduled_day.sql`                | create        | Delete legacy plans, drop `daily_plan_items`, add plan snapshot columns, `daily_plan_tasks`, `daily_plan_blocks`, and unique `(owner_id, plan_date)`. | Flyway V3        |
| `backend/src/main/java/com/focusflow/plan/DailyPlan.java`                      | modify        | Window, peak, metrics, requested/realized buffer. Children are tasks and blocks.                                                                      | entity           |
| `backend/src/main/java/com/focusflow/plan/DailyPlanTask.java`                  | create        | One ranked snapshot: identity, display fields, due date, must-include, unplaced remainder. No description.                                            | entity           |
| `backend/src/main/java/com/focusflow/plan/DailyPlanBlock.java`                 | create        | One clock interval. Work blocks reference a plan task.                                                                                                | entity           |
| `backend/src/main/java/com/focusflow/plan/DailyPlanItem.java`                  | delete        | Replaced by task plus block.                                                                                                                          | —                |
| `backend/src/main/java/com/focusflow/plan/DailyPlanRepository.java`            | modify        | One plan per owner and date. Summary projection without `availableMinutes`.                                                                           | repository       |
| `backend/src/main/java/com/focusflow/plan/DailyPlanSummaryProjection.java`     | modify        | Scheduled minutes, session count, task count, unplaced count, derived warning.                                                                        | projection       |
| `backend/src/main/java/com/focusflow/plan/DailyPlanService.java`               | modify        | Non-transactional orchestration: precondition, cap, ranking, both stages, postconditions.                                                             | `generate`       |
| `backend/src/main/java/com/focusflow/plan/DailyPlanPersister.java`             | modify        | Lock owner, recheck replace, reload task references, replace with a new plan id.                                                                      | `persist`        |
| `backend/src/main/java/com/focusflow/plan/DailyPlanResponseMapper.java`        | modify        | `blocks`, `unplacedWork`, derived warning, session index/count.                                                                                       | mapper           |
| `backend/src/main/java/com/focusflow/plan/DailyPlanController.java`            | modify        | `/by-date` replaces `/latest`. Delete takes the owner lock.                                                                                           | REST             |
| `backend/src/main/java/com/focusflow/plan/dto/GeneratePlanRequest.java`        | modify        | Required `planDate`, optional `replacePlanId`. No `availableMinutes`.                                                                                 | request DTO      |
| `backend/src/main/java/com/focusflow/plan/dto/DailyPlanResponse.java`          | modify        | Schedule snapshot, metrics, buffer pair, warning, blocks, unplaced work.                                                                              | response DTO     |
| `backend/src/main/java/com/focusflow/plan/dto/DailyPlanItemResponse.java`      | delete        | Replaced by block and unplaced-work responses.                                                                                                        | —                |
| `backend/src/main/java/com/focusflow/plan/dto/ScheduledBlockResponse.java`     | create        | Discriminated placed block.                                                                                                                           | response DTO     |
| `backend/src/main/java/com/focusflow/plan/dto/UnplacedWorkResponse.java`       | create        | Remainder view of a plan task.                                                                                                                        | response DTO     |
| `backend/src/main/java/com/focusflow/plan/dto/TaskSnapshotResponse.java`       | create        | Source id, nullable live id, title, priority, status, due date, estimate, must-include.                                                               | response DTO     |
| `backend/src/main/java/com/focusflow/plan/dto/DailyPlanSummaryResponse.java`   | modify        | Drop `availableMinutes` and generic item count.                                                                                                       | response DTO     |
| `backend/src/main/java/com/focusflow/plan/dto/DailyPlanWarning.java`           | modify        | Derived from unplaced must-include plan tasks.                                                                                                        | response DTO     |
| `backend/src/main/java/com/focusflow/plan/DailyPlanWarningSnapshot.java`       | delete        | Warning is no longer stored.                                                                                                                          | —                |
| `backend/src/main/java/com/focusflow/ai/AiDailyPlanRequest.java`               | modify        | Drop `availableMinutes`.                                                                                                                              | AI request       |
| `backend/src/main/java/com/focusflow/ai/AiDailyPlanResponse.java`              | modify        | `{ taskIds }` in array order.                                                                                                                         | AI response      |
| `backend/src/main/java/com/focusflow/ai/OpenAiDailyPlanClient.java`            | modify        | Parse the ordered-id payload.                                                                                                                         | client           |
| `backend/src/main/java/com/focusflow/common/error/ApiErrorResponse.java`       | modify        | Nullable stable `code`.                                                                                                                               | error body       |
| `backend/src/main/java/com/focusflow/common/error/ConflictException.java`      | modify        | Carry an optional code. Existing message-only throws stay valid.                                                                                      | 409              |
| `backend/src/main/java/com/focusflow/common/error/GlobalExceptionHandler.java` | modify        | Copy `code` onto the response.                                                                                                                        | HTTP errors      |
| `README.md`                                                                    | modify        | Backup-first warning: V3 deletes every existing Daily plan.                                                                                           | contributor docs |




### Slice G — Frontend


| Path                                                              | Create/Modify | Responsibility                                                                                                         | Public surface   |
| ----------------------------------------------------------------- | ------------- | ---------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `frontend/src/types/api.ts`                                       | modify        | Preferences, commitments, scheduled plan, warning, and nullable error `code`.                                          | shared types     |
| `frontend/src/lib/api.ts`                                         | modify        | Expose `code` on `ApiError`.                                                                                           | `ApiError.code`  |
| `frontend/src/features/preferences/api.ts`                        | create        | GET/PUT scheduling preferences.                                                                                        | API helpers      |
| `frontend/src/features/preferences/hooks.ts`                      | create        | Settings query and save mutation.                                                                                      | hooks            |
| `frontend/src/features/preferences/SchedulingPreferencesForm.tsx` | create        | Window, cadence, minimum session, buffer, peak, fixed breaks.                                                          | settings form    |
| `frontend/src/routes/SettingsPage.tsx`                            | create        | Authenticated settings page.                                                                                           | `/settings`      |
| `frontend/src/App.tsx`                                            | modify        | Register `/settings`.                                                                                                  | routes           |
| `frontend/src/components/layout/AppLayout.tsx`                    | modify        | Settings nav link.                                                                                                     | sidebar          |
| `frontend/src/features/commitments/api.ts`                        | create        | List, create, update, delete.                                                                                          | API helpers      |
| `frontend/src/features/commitments/hooks.ts`                      | create        | Date-scoped commitment query.                                                                                          | hooks            |
| `frontend/src/features/commitments/CommitmentList.tsx`            | create        | Add, edit, move date, confirm delete.                                                                                  | dashboard column |
| `frontend/src/features/plans/api.ts`                              | modify        | `/by-date`, explicit `planDate`, optional `replacePlanId`. Remove hardcoded today from generate.                       | API helpers      |
| `frontend/src/features/plans/hooks.ts`                            | modify        | Query key includes the selected date.                                                                                  | hooks            |
| `frontend/src/features/plans/GeneratePlanCard.tsx`                | modify        | No minute input. On `PLAN_EXISTS` or `PLAN_CHANGED`, refetch `/by-date`, then confirm replace or offer a fresh create. | generate card    |
| `frontend/src/features/plans/DayTimeline.tsx`                     | create        | Vertical rail: proportional blocks, peak band, keyboard labels, reduced-buffer note.                                   | rail             |
| `frontend/src/features/plans/DailyPlanView.tsx`                   | modify        | Compose warning, rail, and unplaced companion list.                                                                    | plan view        |
| `frontend/src/features/plans/PlanHistoryList.tsx`                 | modify        | Scheduled minutes, sessions, tasks, unplaced count, warning.                                                           | history rows     |
| `frontend/src/routes/DashboardPage.tsx`                           | modify        | `?date=` planning date drives plan, commitments, and generate.                                                         | `/dashboard`     |
| `frontend/src/routes/PlanDetailPage.tsx`                          | modify        | 404 sends the user to `/dashboard?date=` when history cache has that date, otherwise to plan history.                  | `/plans/:id`     |


`DayTimeline` is a split of `DailyPlanView` because the rail has its own keyboard and geometry behavior. Do not grow a second plan feature folder.

### Slice H — Contract docs


| Path                   | Create/Modify | Responsibility                                                           | Public surface     |
| ---------------------- | ------------- | ------------------------------------------------------------------------ | ------------------ |
| `docs/openapi.yaml`    | modify        | Regenerated snapshot after the HTTP surface settles.                     | OpenAPI            |
| `docs/api.md`          | modify        | Replace `/latest` and available-minutes with the scheduled-day contract. | API docs           |
| `docs/architecture.md` | modify        | Plan task, plan block, preferences, commitments, and `schedule`.         | architecture notes |




### Blast radius


| Path                                                       | Why sensitive                                                                                         | Plan mode (before implementation)                                               |
| ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| `V2__scheduling_foundations.sql` / `V3__scheduled_day.sql` | Forward-only. V3 deletes every Daily plan. `V1` must stay valid for existing databases.               | high — confirm backup copy and that Hibernate `validate` matches both versions  |
| `DailyPlanService` / `DailyPlanPersister`                  | Generation is the product path. 1.1.0's short transaction and "save nothing on failure" must survive. | high — map the compare-and-replace matrix before editing                        |
| `ApiErrorResponse` / `ConflictException`                   | Shared by auth, tasks, and plans. New `code` stays nullable so current errors do not change shape.    | high — existing message-only 409s keep working                                  |
| `UserRepository` owner lock                                | Serializes plan, commitment, and preference writes. Task CRUD must not take it.                       | high — lock scope matches ADR 0025                                              |
| `tasks.estimated_minutes`                                  | V2 rewrites legacy `<= 0` to null. Task create/update must match the check.                           | high — no guessed replacement values                                            |
| `frontend/src/types/api.ts`                                | Every plan and task screen reads these types.                                                         | high — switch call sites in the same frontend slice that consumes each contract |
| `docs/openapi.yaml`                                        | Freshness test fails if the snapshot drifts.                                                          | medium — regenerate only after T10, from the existing update task               |




## Workflow (for implementers)

1. **writing-plans** produced this file (type-1 decomposition only).
2. For each subtask: **Plan mode** + **planning-subtasks** skill → type-2 plan (`.cursor/plans/*.plan.md`) when **Plan mode** is `high` or `medium`.
3. **Agent mode**: **test-driven-development** when `TDD suitable: yes` (or the TDD slice of `partial`); match **Verification** when `no`.
4. Update this document if reality diverges; add a **Plan changelog** row.



## Subtasks

Dependency notation: `Blocked by: T1` means start after T1 is done.

### T1 — Additive scheduling schema

- [x] **Do:** Add Flyway V2 for `scheduling_preferences`, `fixed_breaks`, and `commitments`, and normalize `tasks.estimated_minutes <= 0` to null before the null-or-positive check. Leave `daily_plans` and `daily_plan_items` unchanged so current generation still runs.

- **Blocked by:** —
- **Plan mode:** high
- **TDD suitable:** partial
- **TDD suitable reason:** V2 SQL is declarative schema. The automated check is migrate-then-validate, plus an assertion that legacy non-positive estimates become null. That is not a red/green cycle on the SQL text.
- **Verification:** `cd backend; .\gradlew.bat test --tests "*FocusFlowApplicationTests" --tests "*PostgresIntegrationTest"`



### T2 — Owner scheduling lock

- [x] **Do:** Add a pessimistic lock on the owner row and a single `OwnerSchedulingLock` entry point. Do not call it from task create/update/delete.

- **Blocked by:** —
- **Plan mode:** high
- **TDD suitable:** yes
- **Verification:** `cd backend; .\gradlew.bat test --tests "*OwnerSchedulingLockTest" --tests "*PostgresIntegrationTest"`



### T3 — Scheduling preferences API

- [x] **Do:** GET returns in-code defaults with `persisted: false` when no row exists. PUT, under the owner lock, validates the spec rules and replaces the aggregate, including the full fixed-break list. Disabled cadence keeps target and break values but scheduling ignores them.

- **Blocked by:** T1, T2
- **Plan mode:** high
- **TDD suitable:** yes
- **Verification:** `cd backend; .\gradlew.bat test --tests "*SchedulingPreferencesControllerTest" --tests "*SchedulingPreferencesServiceTest" --tests "*LayeredArchitectureTest"`



### T4 — Commitments API

- [x] **Do:** Owner-scoped list, create, update, and delete. Lock the owner, then reject same-date overlap, overnight windows, and non-minute times. Missing or other-user ids return 404. A commitment outside the work window is stored and does not need a plan yet.

- **Blocked by:** T1, T2
- **Plan mode:** high
- **TDD suitable:** yes
- **Verification:** `cd backend; .\gradlew.bat test --tests "*CommitmentControllerTest" --tests "*CommitmentServiceTest"`



### T5 — Positive task estimates

- [x] **Do:** Task create and update accept only null or a positive `estimatedMinutes`. Totals that will become `requiredMinutes` use `long`. Do not invent a cap.

- **Blocked by:** T1
- **Plan mode:** medium
- **TDD suitable:** yes
- **Verification:** `cd backend; .\gradlew.bat test --tests "*TaskControllerTest" --tests "*TaskServiceTest"`



### T6 — Stage 1 free intervals and buffer

- [ ] **Do:** Implement the pure stage-1 planner: clip breaks and commitments, give commitments precedence, reserve the latest free minutes as buffer (splitting around unavailable time), and report requested versus realized buffer. A fully covered day returns no work-free intervals.

- **Blocked by:** —
- **Plan mode:** high
- **TDD suitable:** yes
- **Verification:** `cd backend; .\gradlew.bat test --tests "*FreeIntervalPlannerTest" --tests "*LayeredArchitectureTest"`



### T7 — Stage 2 placement

- [ ] **Do:** Place ranked tasks into stage-1 intervals. Honor look-ahead tail absorb, early cadence breaks, restorative fixed breaks, non-restorative commitments, and the minimum session even when cadence is disabled. Emit `NO_ESTIMATE` and `OUT_OF_TIME` on the ranked task, not as clock blocks.

- **Blocked by:** T6
- **Plan mode:** high
- **TDD suitable:** yes
- **Verification:** `cd backend; .\gradlew.bat test --tests "*WorkPlacementPlannerTest"`



### T8 — Schedule postconditions

- [ ] **Do:** Validate the finished schedule independently of the planners: alignment, order, non-overlap, window bounds, and per-task conservation of estimate versus scheduled work plus unplaced minutes.

- **Blocked by:** T7
- **Plan mode:** medium
- **TDD suitable:** yes
- **Verification:** `cd backend; .\gradlew.bat test --tests "*SchedulePostconditionValidatorTest"`



### T9 — Ordered-id ranking validator

- [ ] **Do:** Add ordered-id validation and the new rejection reasons. Update prompt text and the 500-character description truncation. Do not switch `DailyPlanService` or the live AI response record in this subtask; current generation must still pass.

- **Blocked by:** —
- **Plan mode:** high
- **TDD suitable:** yes
- **Verification:** `cd backend; .\gradlew.bat test --tests "*DailyPlanRankingValidatorTest" --tests "*DailyPlanPromptBuilderTest" --tests "*DailyPlanServiceTest"`



### T10 — Plan cutover and generation

- [ ] **Do:** Land V3 and replace generation with the spec flow: candidate cap, ordered-id provider call, both scheduler stages, postcondition failure as 500 with nothing saved, owner-locked compare-and-replace, new plan id on replace, plan-task snapshots, `/by-date`, derived warning, and nullable error `code` for `PLAN_EXISTS`, `PLAN_CHANGED`, `TASK_MISSING_DURING_GENERATION`, and `PLAN_CANDIDATE_LIMIT`. README states that V3 deletes existing plans and must be preceded by a backup.

- **Blocked by:** T3, T4, T5, T8, T9
- **Plan mode:** high
- **TDD suitable:** yes
- **Verification:** `cd backend; .\gradlew.bat test --tests "*DailyPlanServiceTest" --tests "*DailyPlanPersisterTest" --tests "*DailyPlanControllerTest" --tests "*PostgresIntegrationTest" --tests "*OpenAiDailyPlanClientTest" --tests "*GlobalExceptionHandlerTest"`



### T11 — Settings page

- [ ] **Do:** Add `/settings` and a sidebar link. The form loads effective preferences, explains unsaved defaults, and PUTs the whole aggregate including fixed breaks. Peak-window copy says the band is visual only.

- **Blocked by:** T3
- **Plan mode:** medium
- **TDD suitable:** partial
- **TDD suitable reason:** Save payload, validation messages, and default copy are TDD. Spacing and control layout are visual verification.
- **Verification:** `cd frontend; npm run test:run -- src/features/preferences src/routes/SettingsPage.tsx src/components/layout/AppLayout.test.tsx src/App.auth-routing.test.tsx`



### T12 — Planning date, commitments, and generate conflicts

- [ ] **Do:** Drive dashboard plan, commitments, and generate from `?date=`, defaulting to the browser-local date. Drop the available-minutes input. On `PLAN_EXISTS` or `PLAN_CHANGED`, refetch `/by-date` before confirmation; if the date is empty, offer a fresh create. Task, preference, and commitment edits show a session-local regenerate prompt and do not call the provider.

- **Blocked by:** T4, T10
- **Plan mode:** high
- **TDD suitable:** partial
- **TDD suitable reason:** Date query param, commitment mutations, and coded conflict refetch are TDD. Form layout is visual verification.
- **Verification:** `cd frontend; npm run test:run -- src/features/plans src/features/commitments src/routes/DashboardPage.test.tsx`



### T13 — Day rail, history, and stale plan detail

- [ ] **Do:** Replace the ordered plan list with the accessible vertical rail and unplaced companion list on the dashboard and plan detail. Show the non-error reduced-buffer note. History rows use the new summary counts. A 404 on `/plans/:id` recovers through the cached plan date when present.

- **Blocked by:** T10, T12
- **Plan mode:** high
- **TDD suitable:** partial
- **TDD suitable reason:** Block labels, keyboard focus, reason copy, warning content, and the 404 redirect are TDD. Proportional geometry and colour are visual verification, not pixel assertions.
- **Verification:** `cd frontend; npm run test:run -- src/features/plans src/routes/PlanDetailPage.test.tsx src/routes/PlanHistoryPage.test.tsx`



### T14 — OpenAPI snapshot and docs

- [ ] **Do:** Regenerate `docs/openapi.yaml` from the Testcontainers context after the HTTP contract has settled. Update `docs/api.md` and `docs/architecture.md` to the task/block model, `/by-date`, preferences, and commitments. Ordinary tests must not write the snapshot.

- **Blocked by:** T10
- **Plan mode:** medium
- **TDD suitable:** partial
- **TDD suitable reason:** Snapshot comparison is an automated contract check. The YAML and the prose docs are declarative, not a red/green behavior cycle.
- **Verification:** `cd backend; .\gradlew.bat test --tests "*OpenApiSnapshotTest"` and a second run stays byte-stable. `docs/api.md` no longer documents `/latest` or `availableMinutes`.



### T15 — Full suite

- [ ] **Do:** Run the full backend and frontend suites after the slices above. Fix only failures caused by this milestone.

- **Blocked by:** T11, T13, T14
- **Plan mode:** skip
- **TDD suitable:** no
- **TDD suitable reason:** Verification pass over already-specified behavior, not a new runtime change.
- **Verification:** `cd backend; .\gradlew.bat test` and `cd frontend; npm run test:run`



## TDD note (Agent mode)

Per subtask, obey `TDD suitable`: `yes` means strict **test-driven-development** (red/green/refactor); `partial` applies it only to the testable slice; `no` means do not force test-first—still satisfy **Verification**. Tag using the **test-driven-development** skill situational guide. Type-2 planning → **planning-subtasks** skill.

Suggested Agent-mode order: **T1, T2, T6, and T9** can start together. **T3 and T4** wait for T1 and T2 and may then proceed in parallel; serialize edits to `LayeredArchitectureTest`. **T5** waits for T1. **T7** waits for T6. **T8** waits for T7. **T10** waits for T3, T4, T5, T8, and T9, and is the only subtask that may edit `DailyPlanService` after T9. **T11** waits for T3 and may overlap the scheduler. **T12** waits for T4 and T10. **T13** waits for T12. **T14** waits for T10. **T15** is last.

T10 is one Plan-mode session, then many RED/GREEN pairs. Do not split it into a schema-only change that leaves generation unable to compile.

## Plan changelog


| Date       | Change                                                                                                                                             |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-22 | Initial 1.2.0 type-1 plan from the twice-reviewed spec. Sequences the spec schema as additive V2 plus destructive V3 so earlier slices stay green. |


